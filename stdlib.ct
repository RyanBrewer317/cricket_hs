import stdlib/Maybe

import stdlib/List

def Iter(l):
  l{
    Empty: {
      val: Maybe.None,
      next: Iter(l)
    },
    Has: first-> rest-> {
      val: Maybe.Some(first),
      next: Iter(rest)
    }
  }
